// 공통 API 타입 (JSDoc). 백엔드 공통 응답 래퍼가 확정되면 여기에 반영한다 (docs/API_CONTRACT.md).

/**
 * 오프셋 페이지네이션 요청 파라미터
 * @typedef {Object} PageParams
 * @property {number} page  1부터
 * @property {number} size
 */

export {}
