# 개발 모드(npm run dev) 전용 환경변수.
# true: 백엔드 없이 개발용 시드 데이터로 피드가 동작한다 (디자인/동작 확인용).
# 실제 백엔드(Spring 8081)에 붙이려면 false 로 바꾼다.
# 프로덕션 빌드(npm run build)에는 이 파일이 적용되지 않으므로 항상 실제 API를 호출한다.
VITE_USE_MOCK=false
